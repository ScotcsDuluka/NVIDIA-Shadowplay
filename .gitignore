# Build outputs
**/bin/
**/obj/
**/Debug/
**/Release/
**/x64/
**/*.pdb
**/*.cache
**/*.tmp
**/*.temp
**/__pycache__/
**/*.pyc

# IDE and local user settings
**/.vs/
**/*.user
**/*.suo
**/*.userosscache
**/*.sln.docstates

# NuGet and restored packages
**/packages/
*.nupkg

# OS files
desktop.ini
Thumbs.db
.DS_Store

# Logs and ad-hoc output captures
*.log
output.txt
spike_output.md

# Local-only artifacts (prevent history bloat accidents — see 2026-08 'heavyweight artifacts' cleanup)
# Note: FFmpeg binaries ship via Overlay/API-Core (ignored dir), never as tracked archives
*.lnk
*.mcworld
*.zip
*.ziplog
*.7z
*.msi
Video_Codec_Interface_13.1.15/
test_*.zip

# Test/spike run outputs
spikes/**/out/
spikes/**/logs/
**/TestResults/
**/*.trx

# App/runtime data that should stay local
# (Overlay/build.txt is NOT here on purpose: it is the tracked canonical build
#  counter read/incremented by the Overlay.vbproj IncrementBuild target)
Overlay/NVIDIA_Shadowplay_Data/
Overlay/API-Core/
feedback-logs/

# Local browser-session output of tools/playwright/playwright-mcp-config.json
tools/playwright/browser/

# stray editor/session backup files (cleanup after e032343 committed four of these)
*.bak
*.bak_wake
*.diagbackup
*.tmpwake

# wrangler files
.wrangler
.dev.vars*
!.dev.vars.example
.env*
!.env.example
dist/
Tester/test/scripts/apphost_test/out*
Tester/test/scripts/apphost_test/bin/
Tester/test/scripts/apphost_test/obj/
Tester/test/scripts/apphost_test/out_final
*.mp4
# tracked A/V sync reference asset used by scripts/sync-verify.ps1
!scripts/sync-reference.mp4
/evidence
/dist-installer

# BL-1: build-emitted provenance artifact (regenerated every build)
/installer/bl1-commit.txt
# Duluka Server local data
Duluka/**/AppData/

# Local secrets (never commit)
Duluka/.server-secret

# Node runtime (Backend)
node_modules/
Backend/data/state.json
Backend/data/hardware-floor.json
Backend/data/logs/
build-all.log

# Staged dev-build output (Scripts\build-dev.ps1) — generated, not source.
# Build-Config/ is the layout authority the script reads and stays tracked.
/Build/*
!/Build/Build-Config/

# Heavy runtime payloads (owner tree) — staged by build scripts, never tracked
Project/ShadowPlay/FFmpeg/
Project/Runtime/
IDK This is/

# version.props = generated by Build.exe (source of truth = version.json)
Build/Build-Config/version.props

# WebView2 user data (Build.exe runtime)
Build/Build-Config/webview2/

# UI diagnostics log (Build.exe runtime)
Build/Build-Config/ui-log.txt

# versions.props / versions.json = generated by Build.exe (per-project version map)
Build/Build-Config/versions.props
Build/Build-Config/versions.json

# BL-1 C++ lane: generated version header (hand-stamped version.rc keeps defaults as fallback)
Project/NvOverlay/Cef/src/bl1_version.h
Project/Launcher.Cef/src/bl1_version.h

# secrets
Server/.server-secret
