# NVIDIA-Shadowplay — Status 2026-09-07

## Canonical checkpoint

- Repository: `C:\My Project\NVIDIA-Shadowplay`
- Branch: `Engine-Rebuild-Stabilization`
- HEAD: `be24068`
- Engine audit: `ENGINE-DEEP-FORENSIC-AUDIT-2026-09-07.md`
- Engine verdict: **SAFE (engine surface)**
- Architecture changes in audit: **NONE**

## Engine audit scope

The deep pass covered:

- lifecycle and state machines
- worker/thread ownership
- Start / Stop / Dispose / Restart
- concurrency and race windows
- QPC/timestamp behavior
- queue/backpressure
- capture and NVENC boundaries
- device-loss containment
- configuration propagation
- audio/video isolation
- M1 CaptureSession cleanup
- M2 Ddagrab stop lifecycle

## Regression proof

- CaptureEngine.Tests: **14/14**
- FrameContractTests: **8/8**
- Recording.Tests: **43/43**
- FFmpegTests: **70/70**
- ConfigTruth: **52/52**
- Engine.Concurrency.Tests: **20 passed / 0 failed / 7 skipped × 10 runs**
- Encoder.Tests: **64 passed / 0 failed / 5 skipped**
- Video.Tests: **38 passed / 0 failed / 28 skipped**
- Build: **0 errors**
- Orphan FFmpeg: **0** in every concurrency run

Hardware gate:

The audit machine has Intel UHD and no NVIDIA adapter. Hardware-gated M1/M2
runtime checks therefore remain SKIP and must be re-run on the GTX 1080 Ti.

## Confirmed fixes

- M1 CaptureSession failure cleanup
- M2 Ddagrab stop/join lifecycle
- Encoder swap vs Dispose race
- Legacy capture `_state` race / start TOCTOU
- Ddagrab retry escalation
- LiveMux and AudioEngineMuxSink drop accounting
- ConfigTruth stale UI paths
- F03-B false-saved unvalidated video promotion

## Remaining

Owner/design decisions remain for:

1. RecordingEngine L12 Dispose-timeout behavior
2. NVENC direct cross-device path / `UseSharedHandle`
3. `AudioClockMode` implement-or-remove
4. dead-code and duplicate consolidation
5. reproducible FFmpeg/Redist payload and version authority
6. product CI and installer/payload coupling
7. NVIDIA-machine runtime re-proof for M1/M2

## Concurrent work

Working tree is intentionally dirty. Current modifications are in Duluka Server,
Connect UI, and `Overlay/build.txt`. They are outside this audit and must be preserved.

A current server configuration mismatch was observed: source `appsettings.json`
contains `127.0.0.1:5115`, while `run-server.ps1` still announces port 5000.
The inspected server process was not listening on either port at that moment.
This remains an active server workstream issue, not an Engine regression.

GitHub OAuth source now has the non-secret ClientId; ClientSecret remains runtime-only.
Never place the secret in tracked documentation, memory, logs, or chat.

## Evidence rule

This status distinguishes code proof, deterministic test proof, and hardware runtime proof.
A hardware-gated SKIP must never be reported as PASS.
